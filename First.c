#include<stdio.h>
int main()
{

    printf("Jay Ganesh...");

    return 0;
}

// to compile the code use command = gcc filename.c -o nameof_executable
// to run code = ./nameof_executable (macos)
//                / nameof_executable (linux)
//                nameof_executable(windows)